# Ultralytics YOLO 🚀, AGPL-3.0 license
# COCO128 dataset https://www.kaggle.com/ultralytics/coco128 (first 128 images from COCO train2017) by Ultralytics
# Example usage: yolo train data=coco128.yaml
# parent
# ├── ultralytics
# └── datasets
#     └── coco128  ← downloads here (7 MB)

    #0: corrosion
    #1: crack
    #2: spalling

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
#path: D:/AI_Net/yolov8/ultralytics/data/images  # dataset root dir
train: path/to/images/train  # train images (relative to 'path')
val: path/to/images/val  # val images (relative to 'path')
test: path/to/images/test # test images (optional)

nc: 3
# Classes
names:
  0: corrosion #腐蚀    
  1: crack     #开裂
  2: spalling  #脱落
